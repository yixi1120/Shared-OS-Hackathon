# 冠盛 V2 交付说明

## 已完成

1. Word、Catalog、Pitch、FAQ、销售规则和提交服务清单统一使用 arena-fixed-v1：两项服务常规与首购6 credits、底价5；预算5直接报价5，低于5拒绝报价。不再使用旧价格作为现售政策。
2. Risk Report 对齐思棋严格 Schema，完整覆盖13个风险标记及 interpretation.flags 的 flag、zh、en。成功和失败示例已重新从真实 API 生成。
3. `sharedos-aligned/docs/guansheng` 包含 catalog.json、pitch.json、faq.json、sales.json、pricing_policy.json、risk_flags.json、独立Schema和同源Markdown，代码可直接读取。
4. `sharedos-aligned/dashboard` 包含全部前端源码；`run_dashboard.py` 与实际Seller同源运行。页面真实执行目录、报价、可选议价、下单、交付和订单查询，不用报告快照替代API。
5. Discord、node ID、purpose、线上服务和产品Agent地址集中在 submission.json，保留null，等待雅婷确认后同步Word与提交正文。

## 验证结果

- 48项针对本次范围的API、定价、报告合同与telemetry回归通过；有1条第三方弃用警告。
- 30项真实本地HTTP及JSON Schema检查通过。
- 7项浏览器检查通过：成功98、失败43、缺凭据401清除旧报告、预算4拒绝、预算5下单金额5、反报价不自动下单、离线错误清除旧结果。无页面脚本异常。
- `verification` 保存实际API响应、时间和检查列表、完整页面截图（*-full.png）、用于Word的报告区域截图。演示输入来自思棋固定案例，不代表Arena任务或已结算credits。
- 完整项目测试曾尝试运行，但当前验证环境缺少 langgraph-checkpoint-sqlite；未声明全套测试通过。正确加载本包src后完成上述48项相关回归。请在安装pyproject全部依赖的合并环境执行完整测试。

## 使用和合入

从 `sharedos-aligned/README_GUANSHENG.md` 开始。整合包保留思棋原源码，冠盛新增目录、前端和运行适配入口，不覆盖原算法和价格政策。历史审查文档可能提到旧价格的来源，仅供追溯，现售政策以PRICING_DECISION和arena-fixed-v1为准。

本次未上传GitHub、未修改main，也未部署线上。合入最新仓库时先核对思棋实现，再应用冠盛新增文件；不要以完整压缩包直接覆盖队友的新提交。Agent Brain材料已可读取，但子洋流程接入签收、雅婷线上部署及平台授权验证仍需团队完成。
